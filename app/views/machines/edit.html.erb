<% if logged_in? %>
  <div class="row">
    <div class="col-xs-12 col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3">
      <h2>マシン編集</h2>
      <%= render 'form' %>
    </div>
  </div>
  <div class = "text-right">
    <%= link_to 'このマシンを削除する', @machine, method: :delete, data: { confirm: '本当に削除してよろしいですか？' }, class: 'text-danger' %>
  </div>
<% end %>
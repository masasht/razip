<% if logged_in? %>
  <div class="row">
    <aside class="col-sm-offset-2 col-sm-8 col-xs-12">
      <div class="panel panel-default">
        <div class="panel-heading">
          <h2 class="panel-title"><%= @kit.name %> の編集</h2>
        </div>
       
        <%= render 'form',{kit: @kit, makers: @makers} %>

      </div>
    </aside>
  </div>
<% end %>